{
  "name": "jaan-entertainment-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && cp -r frontend/dist ./dist",
    "vercel-build": "cd frontend && npm install && npm run build && cd .. && mkdir -p dist && cp -r frontend/dist/* dist/",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "dev": "npx concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\"",
    "edge:dev": "CLOUD_SERVER_URL=http://localhost:5001 node edge-agent/edgeAgent.js",
    "edge:prod": "CLOUD_SERVER_URL=https://api.e3di.org node edge-agent/edgeAgent.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1041.0",
    "@aws-sdk/s3-request-presigner": "^3.1041.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "fluent-ffmpeg": "^2.1.3",
    "jsonwebtoken": "^9.0.3",
    "mongodb-memory-server": "^11.0.1",
    "mongoose": "^8.10.1",
    "multer": "^1.4.5-lts.1",
    "multer-s3": "^3.0.1",
    "node-cron": "^4.2.1",
    "otplib": "^13.4.0",
    "socket.io": "^4.8.3"
  }
}
